run:
  timeout: 5m

linters:
  enable:
    - misspell
    - unconvert

issues:
  exclude-rules:
    # httptest handlers and test helpers conventionally ignore Write/Encode
    # error returns; errcheck noise there hides real findings.
    - path: _test\.go
      linters:
        - errcheck
